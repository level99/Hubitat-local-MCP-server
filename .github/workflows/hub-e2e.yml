name: Hub E2E (test hub)

# End-to-end tests against a real Hubitat hub provided by @level99
# (see issue #77 + the GitHub secret LEVEL99_TEST_HUB_MCP_URL).
#
# Failure mode: BLOCKING. Per the maintainer this check is required for merge --
# a red e2e means the change could not be proven against a real hub, so it gates.
#
# Trigger is pull_request_target (NOT pull_request) so the run executes in the base
# repo's trusted context and therefore has the hub secret even for fork PRs -- the
# only way to auto-run e2e on a contributor's fork. The workflow file itself always
# comes from the base branch under pull_request_target, so a PR cannot alter the CI
# logic; it can only change the app source / tests that get deployed and run, which
# is gated for untrusted authors by the `approve` job below.
#
# Who runs automatically vs. who waits for approval is decided by the `approve` job's
# environment selection. When the hub secret genuinely isn't configured (someone
# forked the whole project without a donated test hub) the e2e job skips cleanly
# rather than failing -- see the secret gate inside it. A confirmed infra/flake
# failure is triaged by the maintainer, not auto-ignored.
#
# ============================ TWO-LANE FLOW (issue #237) ============================
# This job runs in one of two LANES, chosen by whether a full-run label is on the PR
# (release:patch | release:minor | release:major | e2e:full):
#
#   FOCUSED lane (no label)  -> runs ONLY the e2e groups affected by the changed files
#                               (the file->group map in .github/scripts/e2e_scope.py) + a
#                               smoke core. Fast iteration signal. NOT the merge gate.
#   FULL lane    (label on)  -> runs the WHOLE suite. This is what the required gate needs.
#
# The REQUIRED merge gate is a POSTED COMMIT STATUS named "Full e2e (runs with label)" -- NOT this job's
# own check ("e2e (run)") -- on purpose, so the focused lane can HOLD the merge without green-washing it:
#   - Iterating (focused lane): the gate is posted PENDING -> YELLOW, holds the merge. NOT skipped (a
#     skipped required check counts as PASS and would let a focused-only PR merge) and NOT red (nothing
#     failed) -- just pending until the full lane runs.
#   - Add a release:*/e2e:full label -> the FULL lane runs -> the gate is posted success/failure.
#     ONLY a green FULL run makes the PR mergeable.
#   - Docs-only / no-secret PRs: the gate is posted success without running (the gate skip path).
#
# >>> A PENDING "Full e2e (runs with label)" on a PR is NORMAL and intended -- it's the gate waiting for
# >>> the full-run label, NOT a stuck/missing run. Add release:*/e2e:full to run the full suite.
# ===================================================================================

on:
  pull_request_target:
    branches: [main]
    # `labeled`/`unlabeled` are included so the lane-gate job can re-evaluate on a full-run-label
    # (release:patch|minor|major|e2e:full) toggle -- but e2e RE-RUNS only when the toggle FLIPS the
    # lane (focused <-> full); a redundant toggle is a no-op. See the "TWO-LANE FLOW" block below.
    types: [opened, synchronize, reopened, labeled, unlabeled]
    # Deliberately NO `paths:` filter. `e2e` is a REQUIRED status check, and a paths-filtered
    # required check never reports on a PR that doesn't touch those paths -- it sits at
    # "Expected -- waiting for status to be reported" forever, so a docs-only PR can never
    # merge. Instead the workflow always triggers and the e2e job's `gate` step detects whether
    # any hub-relevant file changed: none changed (docs-only / non-hub PR) -> report green
    # WITHOUT running the suite; any changed -> run the real suite. The hub-relevant allowlist
    # lives in that gate step and is the source of truth for "what must exercise e2e" -- do NOT
    # reintroduce a workflow-level paths: filter here (it recreates the never-reports deadlock).
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to test (blank = current ref)'
        required: false
      tests:
        description: 'ONE-OFF: comma-separated test name(s) to run instead of the full suite (e.g. test_set_rule_required_expression_multi_condition). Blank = full suite. Installs the PR head first, same as a normal run.'
        required: false
        default: ''
      skip_install:
        description: 'ONE-OFF: skip installing the PR head — run against whatever is already deployed (usually main). For tests-only changes where the server code is unchanged. Keeps the lease; skips the watchdog arm/install/restore.'
        required: false
        default: 'false'
      probe_only:
        description: 'true = read-only hub inventory probe instead of the e2e suite (diagnose hub state)'
        required: false
        default: 'false'
      reboot_first:
        description: 'probe_only: true = reboot the test hub before probing (clears a stuck per-app load throttle / LimitExceededException)'
        required: false
        default: 'false'
      disable_app_id:
        description: 'probe_only: installed-app id to DISABLE (red-X, not delete) after the probe, via the watchdog hub_set_app_disabled'
        required: false
        default: ''

permissions:
  contents: read
  # statuses: write lets the "Post e2e gate status" step POST the required commit status
  # "Full e2e (runs with label)" (pending while focused, success/failure when full). Without it
  # the POST 403s ("Resource not accessible by integration") and the gate can never report.
  statuses: write

# New-commit-cancel, per branch: a fresh push / reopen / dispatch (and the FIRST full-run label, per
# the group note below) on a branch cancels the older
# in-flight run for that SAME branch, and its replacement re-queues at the BACK of the e2e job's
# FIFO group. Per-branch (head-repo + head_ref) so two forks reusing a branch name don't cancel
# each other, and an unapproved fork PR only ever holds ITS OWN branch's group -- never the shared
# hub or another PR. Cross-PR FIFO serialization is the e2e job's SEPARATE `queue: max` group; the
# two can't be one block (queue:max + cancel-in-progress:true is an invalid combination).
# Cancelling mid-run is safe: the e2e job's disarm/restore + lease-release steps are `if: always()`
# (so a cancel still frees the hub), and a hard kill is backstopped by the dead-man watchdog's
# deadline restore plus the 30-min lease TTL.
# group + cancel-in-progress: a run that should SUPERSEDE another -- a commit/reopen/dispatch, OR the
# FIRST full-run label promoting a live FOCUSED run to full -- shares the per-branch group, so
# cancel-in-progress (true) cancels the older run. EVERY OTHER label event -- a redundant full label,
# a non-lane label, or a DEMOTE (the last full label removed) -- gets an ISOLATED per-run group so it
# can neither cancel nor EVICT another run. That isolation is load-bearing: the per-branch group is
# queue:single, so a no-op label run entering the SHARED group would EVICT a pending demote/flip run
# (pending eviction happens regardless of cancel-in-progress), silently dropping a run that still owed
# the gate a status. The "shares the group" test below is the FIRST-full-run-label condition (GitHub
# has no arithmetic, so it's enumerated: the added label is full AND no OTHER full label is present);
# it is a strict SUBSET of lane_gate.js's run decision, so a shared run never cancels a run that then
# skips. It MUST stay in lockstep with lane_gate.js (same full-run-label set). DEMOTE: removing the
# last full label re-runs focused in its OWN isolated group (never evicted); it does not cancel the
# in-flight full run, just queues behind it in the e2e job's hub-e2e-serialized group and posts the
# pending gate after it.
# MANUAL RE-RUNS (github.run_attempt > 1) take the ISOLATED group too, for EVERY event type: a
# re-run replays a FROZEN payload, so an old superseded commit run re-entering the shared per-branch
# group would cancel the CURRENT head's in-flight run (cancel-in-progress) or evict a pending one
# (queue:single) -- an obsolete run must never displace a live one. Isolated, the re-run cancels
# nothing and just queues FIFO in the e2e job's hub-e2e-serialized group; lane-gate honors it via
# the attempt>1 override in lane_gate.js. The attempt==1 guard only SHRINKS the shared set, so the
# subset invariant above still holds. If run_attempt were ever stale at re-run time the expression
# degrades to the old per-event grouping -- never a new hazard.
concurrency:
  group: >-
    ${{ (github.run_attempt == 1
    && (github.event_name != 'pull_request_target'
    || (github.event.action != 'labeled' && github.event.action != 'unlabeled')
    || (github.event.action == 'labeled' && (
    (github.event.label.name == 'release:patch' && !contains(github.event.pull_request.labels.*.name, 'release:minor') && !contains(github.event.pull_request.labels.*.name, 'release:major') && !contains(github.event.pull_request.labels.*.name, 'e2e:full'))
    || (github.event.label.name == 'release:minor' && !contains(github.event.pull_request.labels.*.name, 'release:patch') && !contains(github.event.pull_request.labels.*.name, 'release:major') && !contains(github.event.pull_request.labels.*.name, 'e2e:full'))
    || (github.event.label.name == 'release:major' && !contains(github.event.pull_request.labels.*.name, 'release:patch') && !contains(github.event.pull_request.labels.*.name, 'release:minor') && !contains(github.event.pull_request.labels.*.name, 'e2e:full'))
    || (github.event.label.name == 'e2e:full' && !contains(github.event.pull_request.labels.*.name, 'release:patch') && !contains(github.event.pull_request.labels.*.name, 'release:minor') && !contains(github.event.pull_request.labels.*.name, 'release:major'))))))
    && format('hub-e2e-{0}-{1}', github.event.pull_request.head.repo.full_name || github.repository, github.head_ref || github.ref_name)
    || format('hub-e2e-{0}-{1}-isolated-{2}', github.event.pull_request.head.repo.full_name || github.repository, github.head_ref || github.ref_name, github.run_id) }}
  cancel-in-progress: true

jobs:
  # The hub-less self-deploy recovery guard (issue #237) is NOT here: running untrusted
  # PR code in this privileged pull_request_target workflow is a cache-poisoning vector,
  # so it lives in the unprivileged self-deploy-recovery.yml (pull_request) instead --
  # it needs no secret and no hub, and runs on every PR there in a safe context.

  # Lane-change gate. A LABEL event (labeled/unlabeled) re-runs e2e ONLY when it actually flips the
  # lane (focused <-> full). Adding a 2nd full-run label (release:patch|minor|major|e2e:full) when
  # the PR is ALREADY full, or removing one while another remains, leaves the lane unchanged -- the
  # prior run's result still stands, so this run skips (no redundant re-deploy). The FIRST full-run
  # label (focused->full) and removing the LAST one (full->focused) DO re-run, as does every
  # non-label event (opened/synchronize/reopened/dispatch). `approve` (and thus `e2e`) gate on this
  # output (probe is dispatch-only, where lane-gate is always true, so it is never label-gated in
  # practice). lane-gate ITSELF always runs; on a skip every DOWNSTREAM job is skipped and posts
  # nothing, so the prior posted gate status is untouched.
  lane-gate:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.decide.outputs.run }}
    steps:
      # Checkout is the BASE ref under pull_request_target (trusted) -- the lane decision gates the
      # `approve` job, so it must run base's logic, never the PR's. The decision is a pure function in
      # .github/scripts/lane_gate.js, unit-tested by tests/lane_gate_test.js (+ lane-gate-test.yml).
      # It MUST stay in lockstep with the cancel-in-progress expression's first-full-label test above.
      - uses: actions/checkout@v7
      - id: decide
        run: node .github/scripts/lane_gate.js

  # Approval gate. The environment NAME is chosen from the PR author's relationship to
  # the repo:
  #   - trusted (repo owner / org member / write-access collaborator, plus @level99 and
  #     @kingpanther13 named explicitly, and any maintainer workflow_dispatch) -> the
  #     `e2e-auto` environment, which has no protection rules, so this job passes
  #     immediately and e2e runs automatically.
  #   - everyone else -> the `e2e-approval` environment, whose required-reviewer rule
  #     pauses the run behind GitHub's "Review pending deployments" button until the
  #     maintainer approves. Permanent (every run, not just first-time), e2e-only.
  # author_association is computed server-side and can't be spoofed, and the workflow
  # file is taken from the base branch under pull_request_target, so a PR can't edit
  # this gate to promote itself.
  #
  # SEPARATE job from `e2e` on purpose: it holds no hub lease and is NOT in the e2e job's cross-PR
  # FIFO group (hub-e2e-serialized), so an outside PR sitting unapproved can't stall the shared test
  # hub for trusted runs -- it's only in the workflow-level per-branch group, which is isolated per
  # branch. e2e starts only once this job succeeds (`needs`).
  approve:
    needs: [lane-gate]
    if: needs.lane-gate.outputs.run == 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ (github.event_name == 'workflow_dispatch' || github.event.pull_request.author_association == 'OWNER' || github.event.pull_request.author_association == 'MEMBER' || github.event.pull_request.author_association == 'COLLABORATOR' || github.event.pull_request.user.login == 'level99' || github.event.pull_request.user.login == 'kingpanther13') && 'e2e-auto' || 'e2e-approval' }}
    steps:
      - name: Gate
        run: echo "e2e approved to run against the shared test hub"

  # Read-only hub inventory dump (dispatch with probe_only=true). Diagnoses test-hub
  # state -- devices, apps, rules, jobs, memory, watchdog flags -- without deploying or
  # mutating anything beyond one labeled command round-trip on the BAT scaffold switch.
  # Shares the e2e job's hub-e2e-serialized concurrency group so it never overlaps a live e2e run.
  probe:
    needs: [approve]
    if: inputs.probe_only == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: hub-e2e-serialized
      # queue:max (matching the e2e job) so a probe dispatch QUEUES behind a pending e2e run rather
      # than evicting it -- the group's default queue:single would let a probe cancel a pending PR's
      # e2e out of the FIFO line. queue:max + cancel-in-progress:false is valid (only queue:max +
      # cancel-in-progress:true is rejected).
      queue: max
      cancel-in-progress: false
    env:
      MCP_URL: ${{ secrets.LEVEL99_TEST_HUB_MCP_URL }}
      WATCHDOG_URL: ${{ secrets.WATCHDOG_MCP_URL }}
    steps:
      - uses: actions/checkout@v7
      - name: Mask access tokens
        run: |
          for U in "$MCP_URL" "$WATCHDOG_URL"; do
            TOKEN="$(echo "$U" | sed -nE 's|.*access_token=(.*)|\1|p')"
            if [ -n "$TOKEN" ]; then
              echo "::add-mask::$TOKEN"
            fi
          done
      - name: Reboot test hub first (reboot_first=true)
        if: inputs.reboot_first == 'true'
        # Clears the platform's sticky per-app load throttle (LimitExceededException "generates
        # excessive hub load") -- community-confirmed to reset only on reboot. Fired through the
        # normal destructive-ops gateway; the probe below then captures the post-reboot state.
        run: |
          set -euo pipefail
          echo "Sending hub_reboot to the test hub..."
          RESP=$(curl -sS --max-time 60 -X POST "$MCP_URL" -H "Content-Type: application/json" \
            -d '{"jsonrpc":"2.0","id":99,"method":"tools/call","params":{"name":"hub_manage_destructive_ops","arguments":{"tool":"hub_reboot","args":{"confirm":true}}}}' || true)
          echo "reboot response head: $(printf '%s' "$RESP" | head -c 300 | tr '\n' ' ')"
          if printf '%s' "$RESP" | grep -qiE "SAFETY CHECK|BACKUP REQUIRED|Write tools are disabled"; then
            echo "::error::hub_reboot was REFUSED by the destructive-ops gate -- see response above."
            exit 1
          fi
          echo "Waiting 60s for the hub to go down, then polling for recovery (up to ~8 min)..."
          sleep 60
          attempt=1
          until curl -sS --fail --max-time 20 -X POST "$MCP_URL" -H "Content-Type: application/json" \
              -d '{"jsonrpc":"2.0","id":100,"method":"tools/call","params":{"name":"hub_get_info","arguments":{}}}' \
              | jq -e '.result.content[0].text' >/dev/null 2>&1; do
            if [ "$attempt" -ge 32 ]; then
              echo "::error::Test hub did not come back within ~8 min of the reboot."
              exit 1
            fi
            attempt=$((attempt + 1))
            sleep 15
          done
          echo "Hub is back after reboot (poll attempt ${attempt})."
      - name: Probe hub inventory
        run: bash .github/scripts/mcp_probe_hub.sh
      - name: Disable installed app (disable_app_id set)
        if: inputs.disable_app_id != ''
        # Parks an app via its disabled flag (the admin UI red-X) WITHOUT deleting it, through the
        # watchdog's hub_set_app_disabled (POST /installedapp/disable + read-back verification).
        run: |
          set -euo pipefail
          if ! printf '%s' "${{ inputs.disable_app_id }}" | grep -qE '^[0-9]+$'; then
            echo "::error::disable_app_id must be numeric"; exit 1
          fi
          RPC=$(jq -nc --arg id "${{ inputs.disable_app_id }}" \
            '{jsonrpc:"2.0",id:1,method:"tools/call",params:{name:"hub_set_app_disabled",arguments:{appId:$id,disable:true,confirm:true}}}')
          RESP=$(curl -sS --max-time 60 -X POST "$WATCHDOG_URL" -H "Content-Type: application/json" --data-binary "$RPC")
          TEXT=$(printf '%s' "$RESP" | jq -r '.result.content[0].text // empty')
          echo "disable result: $TEXT"
          printf '%s' "$TEXT" | jq -e '.success == true' >/dev/null || {
            echo "::error::hub_set_app_disabled did not verify the flip -- see result above."; exit 1; }

  e2e:
    # This job's OWN check is "e2e (run)" -- it shows the lane that actually ran (focused green/red while
    # iterating, full when labeled; the run summary prints which lane). The REQUIRED merge gate is the
    # SEPARATE posted commit status named "Full e2e (runs with label)" (see TWO-LANE FLOW at top + the
    # "Post e2e gate status" step). Branch protection requires "Full e2e (runs with label)", NOT "e2e (run)".
    name: e2e (run)
    needs: [lane-gate, approve]
    if: inputs.probe_only != 'true' && needs.lane-gate.outputs.run == 'true'
    runs-on: ubuntu-latest
    # Bounds the EXECUTING job. Cross-PR queueing happens BEFORE this clock starts (a run waits its
    # turn in the hub-e2e-serialized concurrency group, and behind the approve gate, both in
    # queued/waiting state -- neither counts against timeout-minutes). So this covers: up to a 4h
    # wait for a HUMAN-held lease to free (LEASE_WAIT_TIMEOUT_S in lease_acquire.sh) + the ~30-min
    # post-acquisition run window. The lease is claimed only AFTER that wait, so the run itself
    # always stays within the 30-min lease TTL.
    timeout-minutes: 270

    # Serialization model: GitHub's NATIVE concurrency does the cross-PR FIFO QUEUE; the lease is
    # the hub-side lock (and the only thing that can see a HUMAN holding the hub by hand).
    #   * `queue: max` lets up to 100 e2e runs WAIT in this one global group and processes them ONE
    #     AT A TIME in first-in-first-out order (by when each STARTED WAITING on the group). This
    #     job is `needs: [approve]`, so it enters the group only AFTER the approve gate clears
    #     (GitHub's observed behavior: a needs-gated job doesn't start waiting on its own group
    #     until it's eligible to run) -- an approved outside-contributor PR therefore takes its
    #     place at APPROVAL time (not its old creation order), and a release:*/e2e:full label spawns
    #     a new run that enters at the BACK. Both lanes (focused/full) are this same job in this
    #     same group, so they queue together.
    #   * Cancelling a SUPERSEDED run (new commit on the same branch) is the WORKFLOW-level
    #     per-branch group near the top of this file. It can't live here: `queue: max` and
    #     `cancel-in-progress: true` cannot be combined in one concurrency block.
    #   * lease_acquire.sh still claims/releases _TEST_HUB_LEASED_BY, but it NO LONGER orders the
    #     queue (concurrency does). Its remaining job is to block when a HUMAN holds the lease by
    #     hand to take the hub for manual use -- which GitHub concurrency can't see. Because only the
    #     head-of-line run is in_progress, a head run waiting out a human-held lease blocks the whole
    #     queue for that wait (bounded by timeout-minutes above; the human hold is the only thing it
    #     ever waits on now).
    concurrency:
      group: hub-e2e-serialized
      queue: max

    env:
      MCP_URL: ${{ secrets.LEVEL99_TEST_HUB_MCP_URL }}
      # The standing dead-man watchdog is a SEPARATE on-hub app with its own cloud MCP
      # endpoint (secret WATCHDOG_MCP_URL). The v2 watchdog drives the backup / PR-install /
      # clean-disarm-restore plumbing over THIS url, independent of the MCP server under test
      # ($MCP_URL) -- so a deploy that bricks the server under test can still be restored
      # through the watchdog's own live endpoint.
      WATCHDOG_URL: ${{ secrets.WATCHDOG_MCP_URL }}

    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.pr_number && format('refs/pull/{0}/head', inputs.pr_number) || github.event.pull_request.head.sha || github.ref }}
          # checkout v7 (#2454) blocks checking out a FORK PR head under pull_request_target by default;
          # this job does exactly that on purpose (fork contributors run e2e against the shared test hub,
          # gated by the `approve` environment), so opt back in. Without this the v6->v7 bump would break
          # every fork-PR e2e run at the checkout step. The intentional-and-gated design is the mitigation.
          allow-unsafe-pr-checkout: true

      - name: Gate — secret availability + hub-relevant change detection
        # Two independent reasons to report green WITHOUT running the hub suite. Both write a
        # line to the run summary so a skip-green is never mistaken for a real pass.
        #
        # 1) Secrets absent. MCP_URL / WATCHDOG_URL are empty only when the secrets aren't
        #    configured at all -- e.g. someone forked the project without the donated test hub,
        #    or @level99 has not provisioned the watchdog endpoint. (Under pull_request_target on
        #    the upstream repo the run is in trusted context, so the secrets ARE present for
        #    trusted and approved fork PRs.) GitHub doesn't allow `secrets` in a job-level `if:`,
        #    so the check happens here as a step output that gates every step below.
        # 2) No hub-relevant file changed (docs-only / non-hub PR). The workflow-level `paths:`
        #    filter was removed because `e2e` is a REQUIRED check and a paths-filtered required
        #    check never reports on a docs-only PR (it hangs at "Expected -- waiting" and blocks
        #    merge). So the workflow always triggers and relevance is detected HERE against the
        #    same allowlist. A PR touching ANY allowlist path takes the real-run branch, so this
        #    cannot green-wash a change e2e should test; detection fails SAFE (run on any doubt);
        #    workflow_dispatch always runs the real suite.
        id: gate
        env:
          GH_TOKEN: ${{ github.token }}
          TESTS_INPUT: ${{ inputs.tests }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
        run: |
          if [ -z "$MCP_URL" ]; then
            echo "::notice::LEVEL99_TEST_HUB_MCP_URL secret not available — skipping E2E (expected on repos without the test hub configured)"
            echo "E2E NOT RUN — test-hub secret (LEVEL99_TEST_HUB_MCP_URL) not configured." >> "$GITHUB_STEP_SUMMARY"
            echo "available=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ -z "$WATCHDOG_URL" ]; then
            echo "::notice::WATCHDOG_MCP_URL secret not available — skipping E2E (the v2 dead-man watchdog endpoint is not provisioned yet; expected until @level99 sets WATCHDOG_MCP_URL)"
            echo "E2E NOT RUN — watchdog secret (WATCHDOG_MCP_URL) not configured." >> "$GITHUB_STEP_SUMMARY"
            echo "available=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"   # manual dispatch always runs the real suite
            if [ -n "$TESTS_INPUT" ]; then
              # ONE-OFF lane: run only the named test(s) (still installs the PR head + holds the lease).
              # Validate to test-name characters so the value can't inject into the runner command line.
              if ! printf '%s' "$TESTS_INPUT" | grep -qE '^[A-Za-z0-9_,]+$'; then
                echo "::error::tests input must be comma-separated test names ([A-Za-z0-9_,] only); got: $TESTS_INPUT"
                exit 1
              fi
              echo "lane=oneoff" >> "$GITHUB_OUTPUT"
              echo "tests=$TESTS_INPUT" >> "$GITHUB_OUTPUT"
              echo "E2E LANE: one-off (--tests $TESTS_INPUT)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "lane=full" >> "$GITHUB_OUTPUT"      # dispatch with no tests = the FULL lane
            fi
            exit 0
          fi
          # Dependabot PRs skip EVERY e2e lane -- report the gate green WITHOUT running. Dependabot only
          # bumps dependency versions and NONE reach the hub: the Gradle deps are test-harness only (the hub
          # runs the raw .groovy; packageManifest.json ships only the two top-level files) and github-actions
          # bumps touch CI YAML only, so e2e would redeploy unchanged main code for zero signal. The real
          # validators (Spock, unit-tests, groovy24-parse, ruff) run as usual. available=false -> the
          # "Post e2e gate status" step posts the required "Full e2e (runs with label)" status as SUCCESS.
          # Dependabot is deliberately NOT auto-approved (absent from the `approve` job's e2e-auto list): a
          # human still approves each run, because a CI-machinery bump can affect the e2e flow itself (e.g.
          # actions/checkout v7 blocks the pull_request_target fork-PR checkout this job relies on) and must
          # not be waved through. A maintainer can still force a real run via workflow_dispatch (handled
          # above, before this gate). PR_AUTHOR comes from env (not inline) to keep the login out of the
          # run-command string.
          if [ "$PR_AUTHOR" = "dependabot[bot]" ]; then
            echo "::notice::Dependabot PR — skipping all E2E lanes (force-green; a dependency bump reaches nothing on the hub, so e2e validates nothing here)."
            echo "E2E NOT RUN — Dependabot PR (dependency bump; e2e validates nothing here). Skipped on purpose; the Spock / unit-tests / groovy24-parse / ruff checks cover it." >> "$GITHUB_STEP_SUMMARY"
            echo "available=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Fetch the PR's changed-file list. Fail SAFE: if it can't be read, run the full suite
          # rather than risk green-washing a code PR.
          files="$(gh api --paginate "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files" --jq '.[].filename')" || {
            echo "::warning::Could not list changed files — running the full E2E suite to be safe."
            echo "available=true" >> "$GITHUB_OUTPUT"
            echo "lane=full" >> "$GITHUB_OUTPUT"   # full suite runs here -> gate must post success/failure, not pending
            exit 0
          }
          # Hub-relevant allowlist — the source of truth for "what must exercise e2e".
          # (`case` patterns: `*` matches `/`, so `libraries/*` covers nested paths too.)
          relevant=false
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            case "$f" in
              hubitat-mcp-server.groovy|hubitat-mcp-rule.groovy|e2e-deadman-watchdog.groovy|e2e-deadman-watchdog-v2.groovy|tests/e2e_test.py|.github/workflows/hub-e2e.yml|.github/scripts/lease_acquire.sh|.github/scripts/lease_release.sh|.github/scripts/mcp_setup_env.sh|.github/scripts/mcp_restore_env.sh|.github/scripts/mcp_validate_package_tool.sh|.github/scripts/mcp_watchdog_lib.sh|.github/scripts/mcp_watchdog_deploy.sh|.github/scripts/mcp_arm_watchdog.sh|.github/scripts/mcp_disarm_watchdog.sh|libraries/*|bundles/*)
                relevant=true ;;
            esac
          done <<< "$files"
          if [ "$relevant" = "true" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
            # LANE: full if a release:*/e2e:full label is on the PR, else focused (see TWO-LANE FLOW at top).
            # `|| true`: the step runs under `-eo pipefail`, so a transient gh-api failure here would abort
            # the whole gate; an empty label list just means "no full-run label" -> focused (the safe lane,
            # which only HOLDS the merge pending -- it can't green-wash).
            labels="$( { gh api "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}" --jq '.labels[].name' 2>/dev/null || true; } | tr '\n' ' ')"
            lane=focused
            for L in release:patch release:minor release:major e2e:full; do
              case " $labels " in *" $L "*) lane=full ;; esac
            done
            # Old PR that predates this flow: e2e_scope.py + the runner's --groups support live in the PR's
            # checked-out HEAD, not here, so the focused lane can't run. Fall back to the FULL lane (the old
            # runner handles `python tests/e2e_test.py`) until the PR merges main. (See AGENTS.md "Two-lane e2e".)
            if [ "$lane" = "focused" ] && [ ! -f .github/scripts/e2e_scope.py ]; then
              echo "::notice::e2e_scope.py not in the PR head — focused lane unavailable; running FULL. Merge main into the PR to enable the focused lane."
              lane=full
            fi
            echo "lane=$lane" >> "$GITHUB_OUTPUT"
            echo "E2E LANE: $lane" >> "$GITHUB_STEP_SUMMARY"
            if [ "$lane" = "focused" ]; then
              # Map changed files to affected @test groups, AND pull in the group of any test ADDED to
              # tests/e2e_test.py (so a tests-only change / a boy-scout test runs its group, not just
              # smoke). Greps the file's patch for added `def test_...` lines.
              # The `{ ... || true; }` around grep is REQUIRED: under `-eo pipefail`, a PR that adds NO new
              # test function (the COMMON focused case) makes grep exit 1 and aborts the whole gate. The
              # guard turns "no match" into an empty list (file-group + smoke still run).
              test_funcs="$( { gh api --paginate "repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files" --jq '.[] | select(.filename=="tests/e2e_test.py") | .patch // empty' 2>/dev/null || true; } | { grep -oE '^\+[[:space:]]*def test_[A-Za-z0-9_]+' || true; } | sed -E 's/^\+[[:space:]]*def //' | sort -u | tr '\n' ' ')"
              CHANGED_FILES="$files" CHANGED_TEST_FUNCS="$test_funcs" python .github/scripts/e2e_scope.py
            fi
          else
            echo "::notice::No hub-relevant files changed — reporting E2E green WITHOUT running the suite (docs-only / non-hub PR)."
            echo "E2E NOT RUN — no hub-relevant files changed (docs-only / non-hub PR). Skipped on purpose; this green does NOT mean the e2e suite executed." >> "$GITHUB_STEP_SUMMARY"
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/setup-python@v6
        if: steps.gate.outputs.available == 'true'
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        if: steps.gate.outputs.available == 'true'
        run: pip install requests

      - name: Mask access token in subsequent log lines
        if: steps.gate.outputs.available == 'true'
        # Secrets are auto-masked, but the URL parsing below can split the
        # token out into a separate string that isn't recognized as the
        # original secret value. Force-mask both endpoints' tokens.
        run: |
          for U in "$MCP_URL" "$WATCHDOG_URL"; do
            TOKEN="$(echo "$U" | sed -nE 's|.*access_token=(.*)|\1|p')"
            if [ -n "$TOKEN" ]; then
              echo "::add-mask::$TOKEN"
            fi
          done

      - name: Parse MCP_URL into HUBITAT_* env vars
        if: steps.gate.outputs.available == 'true'
        # tests/e2e_test.py expects these three env vars (see its docstring).
        # MCP_URL format: https://cloud.hubitat.com/api/<UUID>/apps/<APPID>/mcp?access_token=<TOKEN>
        run: |
          # Note: regional cloud endpoints (e.g. eu.cloud.hubitat.com) are not yet supported —
          # update the regex if Hubitat adds them.
          if [[ ! "$MCP_URL" =~ ^https://cloud\.hubitat\.com/api/[^/]+/apps/[0-9]+/mcp\?access_token=.+$ ]]; then
            echo "::error::MCP_URL does not match the expected cloud-endpoint shape — refusing to parse a malformed value into HUBITAT_* env vars."
            exit 1
          fi
          BASE=$(echo "$MCP_URL"  | sed -E 's|(https://cloud\.hubitat\.com/api/[^/]+)/apps/([0-9]+)/mcp\?access_token=(.*)|\1|')
          APPID=$(echo "$MCP_URL" | sed -E 's|(https://cloud\.hubitat\.com/api/[^/]+)/apps/([0-9]+)/mcp\?access_token=(.*)|\2|')
          TOKEN=$(echo "$MCP_URL" | sed -E 's|(https://cloud\.hubitat\.com/api/[^/]+)/apps/([0-9]+)/mcp\?access_token=(.*)|\3|')
          {
            echo "HUBITAT_HUB_URL=$BASE"
            echo "HUBITAT_APP_ID=$APPID"
            echo "HUBITAT_ACCESS_TOKEN=$TOKEN"
          } >> "$GITHUB_ENV"

      - name: Watchdog health check (HALT if unreachable / not installed)
        if: steps.gate.outputs.available == 'true'
        # The v2 dead-man watchdog is a STANDALONE on-hub app reachable on its own cloud MCP
        # endpoint ($WATCHDOG_URL) -- the same tool surface as the main server (hub_get_info,
        # tools/list, etc.). Everything below (backup+arm, PR install, clean-disarm restore)
        # drives the hub THROUGH that endpoint, so if it is unreachable or the watchdog app
        # isn't installed there is no safety net and we must HALT before mutating the hub --
        # never deploy PR code we can't roll back. NOT continue-on-error: a dead watchdog
        # endpoint is a hard stop, not a skip.
        run: |
          set -euo pipefail
          echo "Probing the watchdog endpoint (tools/list)..."
          LIST_RPC='{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}'
          RESP=$(curl -sS --max-time 60 -X POST "$WATCHDOG_URL" \
            -H "Content-Type: application/json" \
            --data-binary "$LIST_RPC" || true)
          if ! printf '%s' "$RESP" | jq -e '.result.tools | type == "array"' >/dev/null 2>&1; then
            echo "::error::Watchdog endpoint ($WATCHDOG_URL) did not return a tools/list result. The v2 dead-man watchdog is unreachable or not installed on the test hub — HALTING before any hub mutation (no safety net to roll back a bad deploy). Body head: $(printf '%s' "$RESP" | head -c 300 | tr '\n' ' ')"
            exit 1
          fi
          echo "Confirming the watchdog responds to hub_get_info..."
          INFO_RPC='{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"hub_get_info","arguments":{}}}'
          INFO=$(curl -sS --max-time 60 -X POST "$WATCHDOG_URL" \
            -H "Content-Type: application/json" \
            --data-binary "$INFO_RPC" || true)
          if ! printf '%s' "$INFO" | jq -e '.result.content[0].text // empty' >/dev/null 2>&1; then
            echo "::error::Watchdog endpoint reachable but hub_get_info returned no result — the watchdog app may be mis-installed. HALTING. Body head: $(printf '%s' "$INFO" | head -c 300 | tr '\n' ' ')"
            exit 1
          fi
          echo "WATCHDOG_HEALTHY — endpoint reachable and hub_get_info OK"

      - name: Acquire test hub lease
        if: steps.gate.outputs.available == 'true'
        env:
          GH_TOKEN: ${{ github.token }}   # lets lease_acquire.sh reclaim a stranded lease whose holding ci-run has finished
        run: bash .github/scripts/lease_acquire.sh "ci-run-${{ github.run_id }}"

      - name: Pre-run hub status probe
        if: steps.gate.outputs.available == 'true'
        # Standard since the 2026-06-11 LimitExceededException hunt: capture the hub's exact
        # pre-run state (memory, jobs, error logs, devices, app instances, live command check)
        # inside our lease window, so a degraded starting state is visible in every run's log.
        # Diagnostic only -- must never fail the run.
        continue-on-error: true
        run: bash .github/scripts/mcp_probe_hub.sh

      - name: Configure test environment (capture pre-state + enable toggles)
        if: steps.gate.outputs.available == 'true'
        # Captures enableCustomRuleEngine / enableRead
        # to $RUNNER_TEMP/mcp_pre_state.json, then enables all three so the
        # tests can exercise the rule engine + hub admin read paths regardless
        # of the test hub's prior toggle state. Refuses if Developer Mode
        # itself is off (UI-only to enable).
        run: bash .github/scripts/mcp_setup_env.sh

      - name: Clear stale watchdog flag
        if: steps.gate.outputs.available == 'true'
        # Best-effort: write {armed:false} to the dead-man flag so a CRASHED prior run's still-armed
        # flag can't fire its auto-restore during THIS run's deploy. continue-on-error because the
        # watchdog/flag may not exist yet (first run on a hub, or watchdog not installed) -- that's
        # fine; there's nothing to clear. The real arm happens later, after a fresh >1MB pre-deploy
        # backup exists. Written over the watchdog's own endpoint ($WATCHDOG_URL).
        continue-on-error: true
        run: |
          curl -sS --max-time 60 -X POST "$WATCHDOG_URL" \
            -H "Content-Type: application/json" \
            --data-binary '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"hub_write_file","arguments":{"fileName":"e2e-deadman-v2.json","content":"{\"armed\":false,\"clearedBy\":\"pre-run-stale-flag-clear\"}","confirm":true}}}' \
            >/dev/null || echo "::notice::stale-flag clear call failed/absent (watchdog/flag may not exist yet) -- continuing"

      - name: Compute PR source URL for importUrl deploy
        if: steps.gate.outputs.available == 'true' && inputs.skip_install != 'true'
        # The hub fetches the PR branch's raw source itself (importUrl mode), so the
        # ~1.5MB blob never crosses the cloud gateway. Resolve head repo + SHA for the
        # three trigger shapes (PR, dispatch-with-pr_number, plain dispatch).
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_NAME: ${{ github.event_name }}
          PR_INPUT: ${{ inputs.pr_number }}
          PR_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          DEFAULT_REPO: ${{ github.repository }}
          DEFAULT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          # pull_request_target carries the same pull_request payload (head repo + sha),
          # so treat it identically to pull_request -- otherwise we'd fall through to the
          # base repo/sha and deploy BASE source instead of the PR's.
          if [ "$EVENT_NAME" = "pull_request" ] || [ "$EVENT_NAME" = "pull_request_target" ]; then
            REPO="$PR_HEAD_REPO"; SHA="$PR_HEAD_SHA"
          elif [ -n "${PR_INPUT:-}" ]; then
            # Validate the dispatch input before it reaches gh api.
            if ! printf '%s' "$PR_INPUT" | grep -qE '^[0-9]+$'; then
              echo "::error::pr_number must be numeric"; exit 1
            fi
            REPO=$(gh api "repos/$DEFAULT_REPO/pulls/$PR_INPUT" --jq '.head.repo.full_name')
            SHA=$(gh api "repos/$DEFAULT_REPO/pulls/$PR_INPUT" --jq '.head.sha')
          else
            REPO="$DEFAULT_REPO"; SHA="$DEFAULT_SHA"
          fi
          # Defense-in-depth: only a 40-hex SHA and an owner/name repo may form the
          # URL, so a malformed value can't inject into $GITHUB_ENV or the URL.
          if ! printf '%s' "$SHA" | grep -qE '^[0-9a-f]{40}$'; then
            echo "::error::unexpected head SHA shape"; exit 1
          fi
          if ! printf '%s' "$REPO" | grep -qE '^[A-Za-z0-9._-]+/[A-Za-z0-9._-]+$'; then
            echo "::error::unexpected head repo shape"; exit 1
          fi
          URL="https://raw.githubusercontent.com/$REPO/$SHA/hubitat-mcp-server.groovy"
          echo "PR_SOURCE_URL=$URL" >> "$GITHUB_ENV"
          # Same validated REPO/SHA, split into the (baseUrl, ref) form hub_update_package
          # takes -- the dev-tool validation step below builds the identical URL on-hub.
          echo "PR_RAW_BASE=https://raw.githubusercontent.com/$REPO" >> "$GITHUB_ENV"
          echo "PR_HEAD_SHA_RESOLVED=$SHA" >> "$GITHUB_ENV"
          echo "::notice::importUrl deploy source: $URL"
          # CANONICAL MAIN: resolve the BASE repo's main-branch HEAD (the known-good baseline the hub
          # should hold). The arm refreshes the hub to this when main's SHA changes or the hub has drifted,
          # so the cache it then takes is canonical main -- not whatever happened to be live. MAIN_CHARS is
          # main's source character length (the deploy landing target); MAIN_SHA gates "did main change?".
          MAIN_SHA=$(gh api "repos/$DEFAULT_REPO/branches/main" --jq '.commit.sha')
          if ! printf '%s' "$MAIN_SHA" | grep -qE '^[0-9a-f]{40}$'; then
            echo "::error::unexpected main-branch SHA shape: $MAIN_SHA"; exit 1
          fi
          MAIN_URL="https://raw.githubusercontent.com/$DEFAULT_REPO/$MAIN_SHA/hubitat-mcp-server.groovy"
          MAIN_CHARS=$(curl -fsSL "$MAIN_URL" | LC_ALL=C.UTF-8 wc -m | tr -d '[:space:]')
          if ! printf '%s' "$MAIN_CHARS" | grep -qE '^[0-9]+$' || [ "$MAIN_CHARS" -lt 100000 ]; then
            echo "::error::canonical main source char count looks wrong ($MAIN_CHARS) -- refusing to drive a main refresh"; exit 1
          fi
          echo "MAIN_SOURCE_URL=$MAIN_URL" >> "$GITHUB_ENV"
          echo "MAIN_SHA=$MAIN_SHA" >> "$GITHUB_ENV"
          echo "MAIN_CHARS=$MAIN_CHARS" >> "$GITHUB_ENV"
          echo "::notice::canonical main: $MAIN_URL (${MAIN_CHARS} chars)"

      # The dead-man watchdog is NOT deployed by e2e: the maintainer manages the watchdog app on the
      # test hub out-of-band (direct hub access). e2e never writes to the watchdog app -- it only drives
      # the standing watchdog's endpoint to arm/install/restore. A PR that changes the watchdog source
      # (e2e-deadman-watchdog-v2.groovy) is validated by WatchdogV2Spec plus that out-of-band deploy,
      # not by an in-run self-update.

      # Backup the known-good MCP server source and ARM the standing v2 dead-man watchdog for THIS
      # run, both driven over the watchdog's own endpoint ($WATCHDOG_URL). The arm script captures a
      # fresh >1MB known-good backup hub-side and asserts it is present before writing the armed flag
      # (ARM_WINDOW_MS deadline, deliberately > the job timeout), so the watchdog can only fire its
      # restore AFTER GitHub has already killed a hung/dead job, never mid-live-run. The always()
      # "Disarm dead-man watchdog (final)" step below clears it and restores main before the lease is
      # released. Placed BEFORE the PR install so the rollback target exists before the hub is mutated.
      - name: Backup + arm dead-man watchdog
        if: steps.gate.outputs.available == 'true' && inputs.skip_install != 'true'
        env:
          PR_RAW_BASE: ${{ env.PR_RAW_BASE }}
          PR_HEAD_SHA_RESOLVED: ${{ env.PR_HEAD_SHA_RESOLVED }}
          MAIN_SOURCE_URL: ${{ env.MAIN_SOURCE_URL }}
          MAIN_SHA: ${{ env.MAIN_SHA }}
          MAIN_CHARS: ${{ env.MAIN_CHARS }}
        run: bash .github/scripts/mcp_arm_watchdog.sh

      # Install THIS PR's source onto the test hub through the watchdog's install plumbing
      # ($WATCHDOG_URL). Replaces the old separate install-libraries / deploy-source / verify-deploy /
      # install-bundle-HPM steps: the watchdog endpoint is a single driver of the same loopback
      # restore/install plumbing, so it installs the package bundle the HPM way (the bundle delivers
      # every #include library -- no separate per-library install), then lands + verifies the app
      # source last (HPM's bundle -> app order) in one step. Blocking:
      # a failed PR install means the tests would run against stale/broken hub code.
      - name: Watchdog - install PR
        if: steps.gate.outputs.available == 'true' && inputs.skip_install != 'true'
        env:
          PR_RAW_BASE: ${{ env.PR_RAW_BASE }}
          PR_HEAD_SHA_RESOLVED: ${{ env.PR_HEAD_SHA_RESOLVED }}
        run: bash .github/scripts/mcp_watchdog_deploy.sh

      - name: Run E2E tests
        id: run_e2e
        if: steps.gate.outputs.available == 'true'
        env:
          # The full lane keeps per-test teardown ON (flag off) so the rule/child corpus stays flat
          # for the whole run. Deferring ~50 fixture deletes to the end lets the corpus grow
          # monotonically -- every later list/create/discovery scales with the accumulated children
          # and ramps the hub into its per-app load limiter partway through the long full run. The
          # focused/one-off lanes are short enough that accumulation never bites, so they keep the
          # deferral (deletes off the test critical path, reaped by the disarm force sweep).
          # NOT set on the post-restore --cleanup-only step below, which stays the idempotent backstop.
          E2E_DEFER_NATIVE_DELETES: ${{ steps.gate.outputs.lane == 'full' && '0' || '1' }}
          E2E_LANE: ${{ steps.gate.outputs.lane }}
          E2E_GROUPS: ${{ steps.gate.outputs.groups }}
          E2E_TESTS: ${{ steps.gate.outputs.tests }}
        run: |
          if [ "$E2E_LANE" = "oneoff" ]; then
            # Maintainer one-off (workflow_dispatch tests=...): run only the named test(s). Gate already
            # validated E2E_TESTS to [A-Za-z0-9_,]. The runner unions --tests into the selection.
            echo "Running a ONE-OFF -- tests: $E2E_TESTS"
            python tests/e2e_test.py --tests "$E2E_TESTS"
          elif [ "$E2E_LANE" = "focused" ]; then
            # The runner treats an EMPTY --groups as "no filter" = the FULL suite, so NEVER pass empty in
            # focused mode (that would silently run everything). e2e_scope.py always emits >=smoke, so this
            # only fires if the gate misbehaved -- fall back to the smoke core, not the whole suite.
            groups="${E2E_GROUPS:-infrastructure,protocol}"
            echo "Running the FOCUSED lane -- groups: $groups"
            python tests/e2e_test.py --groups "$groups"
          else
            echo "Running the FULL lane -- whole suite"
            python tests/e2e_test.py
          fi

      # The install above landed THIS PR's app on the hub, so hub_update_package now
      # exists there. Exercise its discovery path live (dryRun: fetch PR source ->
      # parse #includes -> resolve self app-class) with ZERO writes. This is a REAL
      # blocking gate -- e2e passing is what shows the tool works. It runs AFTER the
      # e2e suite so a tool failure can't skip the suite; the cleanup/restore steps
      # below all use `if: always()`, so the hub is restored regardless. The full
      # write path (real library + #include + compile equivalence) is validated by the
      # watchdog PR-install step above plus the hub_list_libraries (McpRoomsLib present)
      # assertions in tests/e2e_test.py.
      - name: Validate hub_update_package dev tool (dryRun)
        id: pkg_validate
        # Validates the PR's package tool against its installed source -- meaningless (and PR_RAW_BASE
        # is unset) when skip_install ran the test against the already-deployed code.
        if: steps.gate.outputs.available == 'true' && inputs.skip_install != 'true'
        env:
          PR_RAW_BASE: ${{ env.PR_RAW_BASE }}
          PR_HEAD_SHA_RESOLVED: ${{ env.PR_HEAD_SHA_RESOLVED }}
        run: bash .github/scripts/mcp_validate_package_tool.sh

      # Post the REQUIRED gate "Full e2e (runs with label)" as a COMMIT STATUS (NOT this job's own check)
      # so the focused lane can hold the merge PENDING without green-washing it (a skipped/green required
      # check would let a focused-only PR merge -- see TWO-LANE FLOW at top). The job's check is "e2e (run)".
      #   focused lane          -> pending (yellow): holds the merge until the full lane runs.
      #   full lane passed       -> success;   full lane failed -> failure.
      #   docs-only / no secret  -> success (the gate skip path; e2e not required to run).
      - name: Post e2e gate status
        # always() EXCEPT on cancel: a focused run cancelled by the FIRST-full-label promote shares the
        # PR head SHA with the full run it spawned (a label add doesn't change the SHA, unlike a commit),
        # so its PENDING post could land AFTER the full run's success/failure on the SAME status context
        # and strand the required gate. !cancelled() makes a cancelled run skip the post, leaving the
        # status to the run that supersedes it. (The teardown steps below stay always() to free the hub.)
        if: always() && !cancelled()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          SHA="${{ github.event.pull_request.head.sha }}"
          [ -z "$SHA" ] && SHA="${{ github.sha }}"
          AVAIL="${{ steps.gate.outputs.available }}"
          LANE="${{ steps.gate.outputs.lane }}"
          if [ "$AVAIL" != "true" ]; then
            STATE=success; DESC="e2e skipped (no hub-relevant change / no test-hub secret); not required to run"
          elif [ "$LANE" = "full" ]; then
            # pkg_validate is SKIPPED (not failed) under a skip_install one-off dispatch; only an actual
            # failure should fail the gate, so a skipped validate counts as a pass.
            if [ "${{ steps.run_e2e.outcome }}" = "success" ] && [ "${{ steps.pkg_validate.outcome }}" != "failure" ]; then
              STATE=success; DESC="full e2e passed"
            else
              STATE=failure; DESC="full e2e failed"
            fi
          else
            STATE=pending; DESC="e2e pending; no label -- add release:* or e2e:full to run the full suite"
          fi
          echo "Posting gate status 'Full e2e (runs with label)': $STATE -- $DESC"
          # Retry the status POST (like lease_acquire.sh's mcp_call): a transient GitHub API blip on
          # this single call must not fail an otherwise-green run and force a ~20-min re-run just to
          # re-post the required gate. A persistent failure (5 misses) still fails the job honestly.
          for attempt in 1 2 3 4 5; do
            if gh api -X POST "repos/${{ github.repository }}/statuses/$SHA" \
                 -f state="$STATE" -f context="Full e2e (runs with label)" -f description="$DESC" >/dev/null; then
              break
            fi
            if [ "$attempt" = 5 ]; then
              echo "::error::Gate status POST failed after 5 attempts."; exit 1
            fi
            echo "::warning::Gate status POST attempt ${attempt}/5 failed; retrying in 5s..."
            sleep 5
          done

      # Disarm the v2 dead-man watchdog -- this is also what RESTORES main: it pushes the pre-deploy
      # known-good snapshot back through the watchdog's own restore plumbing ($WATCHDOG_URL) (replacing
      # both the old disarm AND the old "Restore previous app source" step). It runs FIRST in teardown,
      # BEFORE the MCP_URL-based cleanup/settings-restore below, on purpose: if the PR compiled but broke
      # the main MCP runtime, those MCP_URL steps would fail against the broken PR app -- restoring main
      # first means they operate on a healthy server. The disarm is FIRE-AND-FORGET: it writes
      # {armed:false,intent:disarm}, reads the flag back and asserts that landed (armed==false +
      # intent==disarm), reaps the deferred native-rule fixtures, and exits. The watchdog restores main
      # asynchronously from the canonical install URLs in the flag manifest; CI does not wait for the
      # restore or assert restoreResult/source-length here. A clean disarm also prevents handing the
      # next run an armed flag.
      - name: Disarm dead-man watchdog (restore main)
        if: always() && steps.gate.outputs.available == 'true' && inputs.skip_install != 'true'
        run: bash .github/scripts/mcp_disarm_watchdog.sh

      - name: Cleanup BAT_E2E_ artifacts
        if: always() && steps.gate.outputs.available == 'true'
        # Defensive — e2e_test.py cleans up after itself in the happy path,
        # but a crashed run can strand virtual devices / rules / variables
        # under the BAT_E2E_ prefix. This sweeps anything left over. Runs AFTER the
        # disarm restored main, so it operates on a healthy server over MCP_URL.
        run: python tests/e2e_test.py --cleanup-only

      - name: Restore test environment (settings back to pre-run state)
        if: always() && steps.gate.outputs.available == 'true'
        # Tolerant of missing pre-state file (early-exit no-op) so a setup
        # failure before capture doesn't propagate here. Runs after the disarm so
        # MCP_URL points at restored main, not a possibly-broken PR app.
        run: bash .github/scripts/mcp_restore_env.sh

      - name: Post-run hub status probe
        # Diagnostic-only forensics worth the ~22s ONLY on a red run -- on a green run nobody reads it,
        # so skip it (gated on run_e2e failure) to keep the held-lease teardown short.
        if: always() && steps.gate.outputs.available == 'true' && steps.run_e2e.outcome == 'failure'
        # The pre-run probe's counterpart: capture what the run LEFT BEHIND (stranded artifacts,
        # load-limiter trips in the error log, memory descent, watchdog flag state) while we still
        # hold the lease. Runs after the cleanup step's restore-completion gate, so the hub is
        # quiet. Diagnostic only -- must never fail the run.
        continue-on-error: true
        run: bash .github/scripts/mcp_probe_hub.sh

      - name: Release test hub lease
        if: always() && steps.gate.outputs.available == 'true'
        run: bash .github/scripts/lease_release.sh "ci-run-${{ github.run_id }}"
